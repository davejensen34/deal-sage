import {AlertTriangle,FileSearch} from 'lucide-react';
import {useSearchParams} from 'react-router-dom';
const errors:Record<string,string>={not_permitted:'This Google account is not permitted for the DealSage pilot.',invalid_callback:'Sign-in could not be completed. Please try again.',expired_session:'Your session expired. Please sign in again.'};
export function Login(){const [params]=useSearchParams(),error=params.get('error');return <main className="login-page"><section className="login-card"><div className="login-brand"><span className="brand-mark"><FileSearch/></span><b>DealSage</b></div><h1>Business ownership and transition intelligence.</h1><p>Build credible ownership understanding from an auditable public-evidence trail.</p>{error&&<div className="login-error"><AlertTriangle/>{errors[error]||'Authentication was cancelled or unavailable.'}</div>}<a className="google-login" href="/api/auth/login">Continue with Google</a><small>Access may be restricted to approved pilot accounts.</small></section></main>}
