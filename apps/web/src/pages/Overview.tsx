import {useQuery} from '@tanstack/react-query';import {ArrowRight,CheckCircle2,FileCheck2,SearchCheck,ShieldAlert,UsersRound} from 'lucide-react';import {Bar,BarChart,CartesianGrid,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {Link} from 'react-router-dom';import {api} from '../api/client';import {Score,Status} from '../components/Status';
export function Overview(){const {data,isLoading}=useQuery({queryKey:['dashboard'],queryFn:()=>api<any>('/dashboard')});if(isLoading)return <div className="loading">Loading research workspace…</div>;const m=data.metrics;const metrics=[['New candidates',m.new,UsersRound],['Needs review',m.needs_review,ShieldAlert],['Validated',m.validated,CheckCircle2],['High confidence',m.high_confidence,SearchCheck],['Evidence items',m.evidence_items,FileCheck2]];return <><div className="page-heading"><div><p className="eyebrow">Research overview</p><h1>Opportunity intelligence, with the evidence attached.</h1><p>Prioritize ownership-transition research without mistaking a signal for a fact.</p></div><Link className="primary" to="/candidates">Review candidates <ArrowRight/></Link></div><div className="metric-grid">{metrics.map(([label,value,Icon]:any)=><div className="metric" key={label}><Icon/><span>{label}</span><b>{value}</b></div>)}</div><div className="dashboard-grid"><section className="panel wide"><div className="panel-title"><div><h2>Research queue</h2><p>Recently updated candidates requiring analyst judgment.</p></div><Link to="/candidates">View all</Link></div><div className="recent-list">{data.recent_candidates.map((c:any)=><Link to={`/candidates/${c.id}`} key={c.id}><div><b>{c.business}</b><span>{c.owner} · {c.city}, {c.state}</span></div><Status value={c.status}/><Score value={c.overall_candidate_confidence}/><ArrowRight/></Link>)}</div></section><section className="panel"><div className="panel-title"><div><h2>Status distribution</h2><p>Current candidate outcomes</p></div></div><div className="chart"><ResponsiveContainer><PieChart><Pie data={data.status_distribution} dataKey="value" nameKey="name" innerRadius={58} outerRadius={83} paddingAngle={3}>{data.status_distribution.map((_:any,i:number)=><Cell key={i} fill={['#1967d2','#ef9f27','#08a07a','#6f55c8','#d65252','#5286b8'][i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="chart-center"><b>{m.total}</b><span>candidates</span></div></div></section><section className="panel"><div className="panel-title"><div><h2>Confidence distribution</h2><p>Deterministic research-priority scores</p></div></div><div className="bar-chart"><ResponsiveContainer><BarChart data={data.confidence_distribution}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="range"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#1967d2" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div><div className="model-note"><b>Average confidence {m.average_confidence}%</b><span>Scores prioritize review; they do not assert fact.</span></div></section></div></>}
