const API=import.meta.env.VITE_API_URL||'';
// Deployments may point VITE_API_URL at either an origin or an already-prefixed
// reverse-proxy path; normalize once so requests never become `/api/api/...`.
const API_ROOT=API.endsWith('/api')?API:`${API}/api`;
function requestHeaders(options?:RequestInit):Headers{const headers=new Headers(options?.headers);headers.set('Content-Type','application/json');if(options?.method&&options.method.toUpperCase()!=='GET')headers.set('X-DealSage-CSRF','1');return headers}
export async function api<T>(path:string, options?:RequestInit):Promise<T>{const response=await fetch(`${API_ROOT}${path}`,{...options,headers:requestHeaders(options)});if(!response.ok){const body=await response.json().catch(()=>({detail:'Request failed'}));throw new Error(body.detail||'Request failed')}return response.json()}
export async function apiDownload(path:string,body:unknown,filename:string):Promise<void>{const response=await fetch(`${API_ROOT}${path}`,{method:'POST',headers:{'Content-Type':'application/json','X-DealSage-CSRF':'1'},body:JSON.stringify(body)});if(!response.ok){const error=await response.json().catch(()=>({detail:'Export failed'}));throw new Error(error.detail||'Export failed')}const url=URL.createObjectURL(await response.blob());const link=document.createElement('a');link.href=url;link.download=filename;link.click();URL.revokeObjectURL(url)}
export type Candidate={id:number;business:string;owner:string;city:string;state:string;signal_type:string;transition_date:string;owner_business_confidence:number;signal_identity_confidence:number;overall_candidate_confidence:number;status:string;updated_at:string};
