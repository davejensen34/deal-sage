# Recent-signal intake

The separately approved September 14 completion envelope now wraps the same frozen intake contract in a shared durable retry budget. `scripts.milestone7_completion_analysis` regenerates the frozen bundle, applies explicitly versioned contract guidance to a copied request, reserves before count/generation calls, and preserves exact effective requests and each result. It does not weaken freshness or ownership gates, rewrite old one-shot protocols, or promote cases. The reviewed local corpus uses content-addressed, non-executable text with explicit Windows newline normalization lineage; observed HTML hashes are not represented as retained raw HTML. See [corrective validation](../research/milestone7-completion-validation.md) for the concrete operator scope and remaining product gaps.

Issue #144 adds `signal-intake-v1` to configured research cases. This is a research-priority and pre-analysis gate, not an ownership score, verified date, or finding that a business is attractive. The existing confidence recency multiplier remains historical evidence weighting; its retrieval fallback cannot qualify an event here.

Create a new case through `ResearchCaseService.create_case(..., signal_intake_policy={"version": "signal-intake-v1", "assessment_date": "2026-09-14", "lookback_days": 90})`. The JSON policy is persisted separately from budgets. Dates must be ISO calendar dates and lookback must be an integer from 1 through 3650. Ninety days is a provisional offline evaluation choice, not an activated product default. No policy means historical behavior; migration `b144f0a9c721` leaves existing cases null. Use a new case for a different assessment window rather than reinterpreting a completed run. Downgrade refuses to discard any configured policy.

Transition claims retain `event_date` and `event_status`; intake also recognizes an optional `announcement_date`. Each supplied date needs its own `event_date_excerpt` or `announcement_date_excerpt` in the claim's `object_value`, exactly contained in that evidence's retained `relevant_excerpt`. Claim creation owns semantic date normalization and attribution. Substring agreement checks provenance, not semantic truth: an analyst must distinguish actual event/announcement language from a dateline, copyright notice or repost. Search-provider dates, publication metadata and retrieval timestamps alone never establish eligibility. Missing support produces date-verification work, not negative ownership evidence.

Routing uses an inclusive `[assessment_date - lookback_days, assessment_date]` interval:

| Route | Meaning | Model analysis eligible |
| --- | --- | --- |
| recent_event | Cited reported/completed event in the window; reported status remains reported | Yes |
| recent_announcement | Cited announcement in the window, event date unknown | Yes, without claiming completion |
| planned_event | Future planned event with a cited recent announcement | Yes, without claiming completion |
| background | Old event/announcement or cancelled event | No, retained as background |
| date_verification | Unsupported/malformed/unknown dates, future non-plan, passed planned date, unresolved conflict | No, retain for investigation |

An old event remains background even after a new publication or announcement. A cited recent event can qualify within an older updated source. Conflicting event dates for the same named subject and signal type, cancellation conflicts, and explicit open claim conflicts require review. This comparison is not identity resolution and may need analyst disambiguation when a subject has multiple separate events. All same-case assertions participate before filtering to a selected packet, preventing date cherry-picking across evidence IDs.

`SearchService` appends bounded date hints and stores the actual submitted query. Provider interpretation is not guaranteed and an empty result remains empty. Date hints apply to all searches in an opted-in case; unrestricted historical/background research should use a separate case until per-action search purposes are implemented. Access decisions and budgets remain in their existing paths.

`ResearchPlanner.start_step` gates model analysis before a step is reserved. `ModelAnalysisService` separately requires an eligible transition among the selected evidence **and claim IDs**, before any provider call. Background evidence can accompany that recent signal. A content-free `signal_intake_analysis` audit records the policy, routes, references and provider/version metadata before an allowed call, including calls that subsequently fail. Case narratives expose current routes and counts. Discovery priority v2 adds 20 points for a qualifying signal or subtracts 15 for wholly historical signals; source access still controls the permissible action. These are heuristics, not calibrated scores.

## Frozen evaluation protocol

`m7-analysis-preparation-v3` and `m7-analysis-execution-v4` apply the same pure routing function as application cases. A `m7-signal-intake-v1` snapshot retains the selected cases' explicit policy, all transition claims, open conflict references, retained evidence excerpts, raw-artifact hashes and source-access attribution. Every case in a cohort must share an assessment policy. Identifiers must be unique and claims must cite same-case evidence. Date assertions remain claim records, not verified dates or human judgments.

The offline freezer opens SQLite with `mode=ro` and `query_only`, verifies retained raw bytes/hash/size/URL lineage, and requires an approved same-case source candidate for every included source. It does not grant new access, establish publisher rights, extract dates, reopen cases or query sources. The first protocol supports these reviewed dynamic-source cases; connector-only evidence without that access lineage is refused. Preparation snapshots all case evidence and transition claims, so a conflicting date cannot be omitted by selecting only a favorable claim. Source semantic review remains necessary: excerpt agreement does not prove that a normalized date is correct.

Run from `apps/api`, using a new output path and explicitly selected configured cases:

```powershell
.venv/Scripts/python.exe -m scripts.prepare_recent_milestone7 --database <SQLite-path> --evidence-dir <retained-evidence-root> --case 21:UT --case 22:CO --output <new-bundle.json>
```

The case IDs above are examples, not a selected or approved cohort. Preparation accepts up to 20 considered cases, records every route/exclusion, and creates requests for at most three eligible cases. More eligible cases require explicit cohort curation; zero eligible cases produce a useful exclusion report with zero requested calls and zero reservation. Old/undated cases are never substituted to fill slots. There is one eligible signal family per analyzed case; background sources can accompany its recent signal. Each source excerpt is bounded, each serialized request is at most 16,000 bytes, and the complete bundle is at most 2 MB. The model receives retained excerpts and source IDs, not normalized date claims, routing judgments or expected outcomes.

Execution requires explicit approval of the exact bundle SHA-256, named public-source transfer and budget, plus `--approved-protocol-id m7-analysis-execution-v4`, `--approved-bundle-sha256 <approved-hash>` and `--confirm-live-calls` on the existing `scripts.run_milestone7_analysis` CLI. These flags record an externally obtained approval; creating a bundle does not provide it. The CLI recomputes the bundle against read-only database/evidence before loading credentials. The executor independently reconstructs every request and route from the snapshot before creating its one-shot claim, reserving cost or counting provider input tokens. Altered routes, requests, limits, hashes and empty cohorts fail before any call. Results carry the intake fingerprint, routes and considered/eligible/excluded denominators separately from model observations and deterministic dispositions.

V4 retains the fixed evaluation model and observation-v2 contract, zero retries, 20,000 input/6,000 output token limits, 90-second client timeout and five-cent reservation per eligible case (at most fifteen cents total). Failed attempts consume their reservation; a failed token-count gate stops remaining calls. No searches or promotions are performed by this executor. These are ceilings for a future separately approved run, not new spending permission. Historical v1/v2 bundles and v2/v3 execution behavior remain preserved, and all old approvals are exhausted. No new real-source cohort was frozen or executed for this implementation.

## Milestone 7 final evaluation

The final Texas packet passed the existing recent-announcement gate while retaining planned ownership transfer and unknown effective date. Completion-only guidance v4 makes successor timing explicit; the operator executor rejects unexpected text controls with a content-free diagnostic after a live NUL-corrupted name. The source bundle and prior model outputs remain unchanged. The separate Texas review package has its own recognized fingerprint, so earlier human feedback cannot apply to it. These are evaluation corrections, not changes to source authority, production promotion or the historical observation contracts. [Closeout and remaining quality gates](../research/milestone7-closeout.md).
