# Shared contracts

Reserved for generated OpenAPI-derived TypeScript contracts when the API stabilizes. Milestone 1 keeps the frontend API boundary explicit without introducing a code-generation build step.
